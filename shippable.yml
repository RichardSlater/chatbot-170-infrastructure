language: none

infra:
  pre_prov:
    - terraform plan

  prov:
    - terraform apply

  post_prov:
    - yes yes 2>/dev/null | terraform destroy -state=/shippableci/shippable/provision/terraform.tfstate

integrations:
  notifications:
    - integrationName: email
      type: email
      recipients:
        - richard.slater@amido.com
      on_success: always
      on_failure: always
      on_start: always
